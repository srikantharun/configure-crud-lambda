# Active scripts only:
#   validate.py          -> boto3   (CloudWatch log queries)
#   build_xlsx.py        -> openpyxl
#   build_comparison.py  -> openpyxl
# The retired mcda_decider.py / policy_scorer.py ML stack
# (prefect, xgboost, scikit-learn, pandas, shap, matplotlib) is no
# longer installed - those scripts live in _archive_* and are off the
# active path.
openpyxl>=3.1.0
boto3>=1.26.0
